# First question: How is my server going to accept request

# Before second question, we need to handle the parsing of the HTTP Message first/

# Second question: How is it going to respond to those request, and handle different routes(endpoints) and methods using the HTTP protocol?